apiVersion: gateway.networkin.k8s.io/v1
kind: Gateway
metadata:
  name: surya-gateway
  namespace: kgateway-ns
spec:
  gatewayClassName: kgateway
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: Same